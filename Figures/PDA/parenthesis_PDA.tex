\begin{tikzpicture}[shorten >=1pt,node distance=3.5cm,on grid,auto]
    \node[state, initial] (q_0)   {$q_0$};
    \node[state] (q_1) [right=of q_0] {$q_1$};
    \node[state, accepting] (q_2) [right=of q_1] {$q_{f}$};

    \path[->]
    % Initialization: Push bottom marker $ and Start Symbol B
    (q_0) edge node {$\epsilon, \epsilon \to B\,\$$} (q_1)

    % The Main Loop: Expand Non-Terminals & Match Terminals
    (q_1) edge [loop above] node {
            \begin{tabular}{r c l}
            % Expansions (matching Grammar Rules)
            $\epsilon, B$ & $\to$ & $(\,B\,)\,B$ \\
            $\epsilon, B$ & $\to$ & $(\,B\,)$ \\
            $\epsilon, B$ & $\to$ & $(\,)\,B$ \\
            $\epsilon, B$ & $\to$ & $(\,)$ \\
            % Matching (Pop terminal if it matches input)
            $(, ($        & $\to$ & $\epsilon$ \\
            $), )$        & $\to$ & $\epsilon$
            \end{tabular}
            } ()

    % Acceptance: Pop bottom marker
            edge node {$\epsilon, \$ \to \epsilon$} (q_2);
\end{tikzpicture}